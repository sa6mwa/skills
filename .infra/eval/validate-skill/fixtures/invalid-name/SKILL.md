---
name: invalid_name
description: Invalid fixture used by validate-skill smoke tests.
---

# Fixture

Expected to fail validation because `name` uses an underscore and does not match directory naming rules.
